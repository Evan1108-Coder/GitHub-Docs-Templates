# Dependabot Templates

`dependabot.yml` tells GitHub how to check dependency updates.

## Where This Goes

Copy one file into your repository and rename it to:

```text
.github/dependabot.yml
```

For example:

```text
DEPENDABOT_npm.yml -> .github/dependabot.yml
```

